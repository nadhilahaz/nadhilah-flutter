part of '../_index.dart';

class ProductAddEcho extends StatelessWidget {
  const ProductAddEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
