part of '../_index.dart';

class ProductAddDelta extends StatelessWidget {
  const ProductAddDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
